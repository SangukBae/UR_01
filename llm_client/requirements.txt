# LLM chat wrapper: an OpenAI-compatible client (Cerebras et al.) + the same
# MCP framework case1/server.py uses, to connect to it in-process.
openai>=1.0
fastmcp>=2.0

# --voice only: microphone capture (numpy, for the energy-based VAD) and
# local speech-to-text (faster-whisper, CPU, no API key). Also needs
# pulseaudio-utils installed system-side (`parec`) -- see voice.py.
numpy>=1.24
faster-whisper>=1.0
